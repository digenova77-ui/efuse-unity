# Unity Measure: Fuel, Residual, and the Empty Center

**Status:** v0.1 claim set — marks only where tip/tests say so  
**Seal:** UNITY_SEAL_v1 (`unity_hash` computed at boot)

## 0. Preamble

Same stick. Empty center. Free will remains. Tip does not rewind.  
This document is a set of **claims**. Protocol facts become **marks** only when evidenced.

## 1. Problem

Title without faced residual is a story. Clearable residual can be paid; the universal floor F_U is not erased by speech.

## 2. Definitions

| Term | Meaning |
|------|---------|
| Mirror | claim vs mark → {0,1} |
| Residual faced | purity |
| Residual refused | sin |
| eFuse | fuel, never Accept |
| pure_own | cost on claim paid before title |
| F_U | floor not erasable by speech |
| F_clearable | payable with fuel + evidence |

## 3. UNITY_SEAL_v1

See `/UNITY_SEAL_v1.md`. Boot refuses mismatch of `unity_hash`.

## 4. Accept pipeline

RF5 → RF6 → GateA → GateB → evidence_ok → spend → tip → Mirror

Fail-closed. No founder Exempt.

## 5. eFuse mechanics

States: **ESCROW → PURE → SPENT**.  
Mint opens a cost claim in ESCROW. Paying cost with Merkle evidence and surviving Accept promotes **PURE**.

## 6–11. Evidence, rails, security, philosophy, limits, roadmap

See repository source and seal. Not a physics TOE. Theorem U1 is system-internal measure unity.

*Fuel pays the claim; the tip keeps the time; the center stays empty.*
